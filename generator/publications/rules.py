from datetime import timedelta

VENTANAS = {
    "oracion": {
        "slug": timedelta(hours=12),
        "texto": timedelta(hours=48),
    },
    "salmo": {
        "slug": timedelta(hours=24),
        "texto": timedelta(hours=72),
    }
}
